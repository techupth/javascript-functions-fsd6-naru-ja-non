// Start coding here
let calculator = {
  add: (a, b) => a + b,
  subtract: (a, b) => a - b,
  multiply: (a, b) => a * b,
  ivide: (a, b) => a / b,
};

let addResult = calculator.add(10, 20);
console.log(addResult);
let divideResult = calculator.add(3000, 10);
console.log(addResult);
